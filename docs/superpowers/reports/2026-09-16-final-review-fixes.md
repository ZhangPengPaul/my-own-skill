# 最终 review 修复记录

日期：2026-09-16。本轮修改保留现有未提交工作，不创建提交。

## 已处理问题

1. **工作区路由**：SKILL 明确仅在工作区不存在时运行 `init_student.py`；已有工作区直接验证，由验证流程迁移缺失的观察目录，不重新初始化。新增路由契约测试，并同步设计文档。
2. **模型评估真实性**：`forward-test-summary.md` 中新增两案例标为 `NOT RUN`，两个判据列均为“未评估”。目录共 14 案例：12 个历史 PASS、2 个待评估。明确历史日期、旧提交范围和当前未提交版本，旧 `d991084` 不覆盖新增案例，当前版本未整体重跑真实模型评估。测试契约核对状态与数量。
3. **全局事实 ID**：快照读取对 session、plan_item 和 observation 的 `record_id` 执行全局唯一检查；即使关闭派生状态一致性检查也不能绕过。回归覆盖三种跨类型组合和两种读取模式。
4. **清理异常**：迁移和删除共用清理函数，独立尝试子目录关闭、锁解锁、锁关闭及根目录关闭；传播首个清理异常，已有业务异常时保留业务异常。故障注入使用真实工作区和真实文件描述符，覆盖单项错误、同时错误、主异常以及调用者处于已有异常处理中的情形。
5. **回归测试质量**：目标不匹配测试使用不同 `interaction_id`，防止被同一互动去重掩盖。新增删除回归从两次观察产生摘要开始，删除后确认待验证摘要消失、观察目录为空且正式状态字节不变。
6. **schema 一致性**：SKILL、设计文档和实现均明确观察 schema_version 为整数 1、record_type 为 interaction_observation、三类事实 record_id 全局唯一；观察字段集合沿用 canonical fixture 和现有校验。

## 验证证据

- 先补测试再改实现：RED 阶段运行 20 个测试，出现 18 个失败断言/子案例，涵盖路由、虚报评估、跨类型 ID 和异常清理。删除摘要及修正后的目标不匹配测试在原实现上通过，属于补齐已有行为的回归覆盖。
- GREEN 针对性验证：104 个测试通过。
- 完整验证：`python3 -m unittest discover -v`，218 个测试通过。
- 差异检查：`git diff --check` 通过。

本轮未运行真实模型执行器；不能据此声称新增两个模型行为案例已通过。历史 12 个 PASS 仅保留为旧版本的评估记录。
